- 采用统一的 `buildTool` 工厂模式构建工具实例，每个工具由核心逻辑文件（如 `EnterWorktreeTool.ts`）、UI 渲染组件（`UI.tsx`）及常量/提示词定义组成。
- 内部依赖 `../../utils/worktree.js` 和 `../../bootstrap/state.js` 管理底层 Git 状态与会话上下文，实现业务逻辑与基础设施的解耦。
- BriefTool 通过动态导入（`import('./upload.js')`）结合 `bun:bundle` 特性实现按需加载，确保非桥接模式下附件上传模块被完全剔除。
- UI 层基于 React 和 Ink 框架，根据会话模式（如 Transcript Mode 或 Brief Only）动态调整消息的视觉呈现结构。