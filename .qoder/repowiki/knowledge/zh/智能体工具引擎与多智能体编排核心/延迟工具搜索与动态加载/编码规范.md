- 采用懒加载 Schema 模式，通过 `lazySchema` 包装 `z.object` 以避免模块初始化时的循环依赖或过早计算。
- 在工具定义中统一使用 `buildTool` 工厂函数，通过 `satisfies ToolDef` 确保类型安全并提供默认的权限与并发处理逻辑。
- 遵循 MCP 工具命名规范，通过解析 `mcp__server__action` 格式的前缀和下划线来增强关键词搜索的准确性。