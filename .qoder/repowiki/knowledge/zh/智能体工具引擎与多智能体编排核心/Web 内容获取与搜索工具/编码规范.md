- 所有工具定义均通过 `buildTool` 工厂函数创建，统一实现 `inputSchema`、`checkPermissions`、`call` 和 UI 渲染方法。
- UI 渲染逻辑拆分为 `renderToolUseMessage`、`renderToolUseProgressMessage` 和 `renderToolResultMessage` 三个独立函数，以支持不同交互状态。
- 使用 `lazySchema` 包装 Zod schema 定义，以避免模块加载时的循环依赖或性能开销。