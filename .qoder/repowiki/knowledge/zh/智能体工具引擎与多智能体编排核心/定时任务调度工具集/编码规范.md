- 所有工具定义均使用 `lazySchema` 包装 Zod 模式对象，以优化启动时的 Schema 初始化性能。
- 工具执行前必须通过 `validateInput` 进行前置校验，包括 Cron 表达式合法性、最大任务数限制及队友上下文权限检查。
- 通过 `isKairosCronEnabled()` 统一 gate 控制所有调度工具的 `isEnabled` 状态，确保功能开关的一致性。