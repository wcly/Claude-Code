- 核心实现位于 `SyntheticOutputTool.ts`，通过 `buildTool` 工厂函数构建工具实例，支持动态注入 JSON Schema 并进行 Ajv 校验。
- 采用 `WeakMap` 对 Schema 进行缓存优化，避免重复编译带来的性能开销。
- `TerminalCaptureTool` 和 `VerifyPlanExecutionTool` 目前仅包含常量定义文件（`prompt.ts`, `constants.ts`），作为工具名称的集中管理点，未包含具体业务逻辑实现。