- 采用 `buildTool` 工厂函数配合 `satisfies ToolDef` 约束来定义工具，确保输入输出 schema 与生命周期方法的类型安全。
- 遵循“逻辑-提示-界面”分离模式，将核心逻辑置于主 `.ts` 文件，模型指令置于 `prompt.ts`，React 渲染组件置于 `UI.tsx`。
- 使用 `lazySchema` 包装 Zod 输入 schema，以优化模块加载性能并避免循环依赖。
- 通过 `constants.ts` 集中导出工具名称等魔术字符串，保持跨模块引用的一致性。