- 所有工具入口均通过 `buildTool` 工厂函数构建，统一实现 `inputSchema`、`outputSchema` 及权限检查接口。
- 文件路径在处理前必须经过 `expandPath` 标准化，并在 UI 展示时通过 `getDisplayPath` 转换为相对路径以提升可读性。
- 写操作（Edit/Write）在执行前必须校验文件是否已被读取（`readFileState`），防止未读先写导致的内容覆盖风险。
- UI 组件采用 React Compiler 优化，并通过 `Suspense` 配合异步数据加载（如差异对比计算）实现非阻塞渲染。