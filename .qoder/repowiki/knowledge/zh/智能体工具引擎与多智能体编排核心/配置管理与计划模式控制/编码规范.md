- 采用 `lazySchema` 包装 Zod 模式定义以优化启动性能并避免循环依赖。
- 使用 `buildTool` 工厂函数统一构建工具实例，集成输入输出模式、权限检查及渲染方法。
- 将工具的显示名称、描述和提示词逻辑抽离至独立的 `constants.ts` 和 `prompt.ts` 文件中。
- 在 `UI.tsx` 中实现标准化的 `renderToolUseMessage` 和 `renderToolResultMessage` 以支持 Ink 终端界面渲染。