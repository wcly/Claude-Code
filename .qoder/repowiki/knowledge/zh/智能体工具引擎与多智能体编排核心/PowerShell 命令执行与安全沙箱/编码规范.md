- 所有 PowerShell 命令名称在内部处理时均统一转换为小写（Canonical Form），以确保大小写不敏感的匹配逻辑一致。
- 安全验证函数统一返回包含 `behavior` 字段（'passthrough' | 'ask' | 'allow'）的结果对象，便于权限引擎链式处理。
- 在处理可能泄露敏感信息的命令（如 `Write-Output`）时，通过 `argLeaksValue` 回调检查参数元素类型，阻止变量或表达式作为参数直接输出。