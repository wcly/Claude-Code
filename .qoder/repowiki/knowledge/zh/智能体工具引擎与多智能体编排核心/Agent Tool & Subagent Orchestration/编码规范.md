- 在 `loadAgentsDir.ts` 中，当代理启用自动记忆 (`memory`) 功能时，会自动向工具列表注入 `Write`、`Edit` 和 `Read` 文件操作工具以确保记忆存取能力。
- Fork 模式下，所有子代理的 `tool_result` 均使用统一的占位符文本 (`FORK_PLACEHOLDER_RESULT`)，以确保不同子代理间的 API 请求前缀字节级一致，最大化提示词缓存命中率。
- 代理定义中的 `getSystemPrompt` 普遍采用闭包或动态函数形式，以便在运行时根据上下文（如记忆快照、环境变量）动态组装系统提示词，而非使用静态字符串。