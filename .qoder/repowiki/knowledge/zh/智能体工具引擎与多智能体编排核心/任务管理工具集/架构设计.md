- 采用模块化设计，每个工具（Create/Get/List）独立封装在各自目录中，包含实现文件、常量定义及提示词。
- 基于 `buildTool` 工厂函数统一构建工具实例，遵循统一的 `ToolDef` 接口规范。
- 依赖 `../../utils/tasks.js` 处理底层任务数据操作，通过 `lazySchema` 实现 Zod 模式的懒加载以优化性能。
- 工具执行结果通过 `mapToolResultToToolResultBlockParam` 统一格式化为可读文本块。