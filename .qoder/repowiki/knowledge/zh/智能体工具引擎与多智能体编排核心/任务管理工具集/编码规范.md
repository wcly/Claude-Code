- 每个工具模块均包含 `constants.ts` 用于导出工具名称常量，以及 `prompt.ts` 用于管理描述与提示词逻辑。
- 输入输出模式均使用 `zod/v4` 的 `lazySchema` 进行定义，确保类型安全并支持循环引用或延迟初始化。
- 所有工具均实现 `isEnabled` 钩子以检查 `isTodoV2Enabled` 状态，并标记 `isConcurrencySafe` 为 true。
- 只读工具（Get/List）显式声明 `isReadOnly() { return true }`，而写操作工具（Create）则省略此声明或设为 false。