- 任务状态更新采用不可变模式，在 `updateTaskState` 回调中通过展开运算符 `{ ...task, status: 'completed' }` 创建新状态对象。
- 所有任务类型均实现 `isXxxTask` 类型守卫函数，用于在联合类型中安全地窄化具体任务状态。
- 任务终止或完成时，通过检查 `notified` 标志位防止向模型或 SDK 发送重复的结束通知。
- 迁移脚本遵循“仅操作用户级设置”原则，避免修改项目级或策略级配置以保证幂等性和安全性。