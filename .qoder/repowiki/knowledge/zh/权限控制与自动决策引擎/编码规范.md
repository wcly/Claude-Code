- 采用“故障关闭”（Fail-closed）的安全原则，当分类器不可用或解析失败时，默认采取拒绝执行或转为人工确认的策略。
- 在 Shell 命令解析中使用带随机盐值的占位符（如 `__SINGLE_QUOTE_{salt}__`）来防止恶意构造的命令字符串在预处理阶段发生注入。
- 权限决策结果统一封装为 `PermissionDecision` 对象，包含行为类型、更新后的输入参数以及决策原因（如规则命中、钩子拦截或分类器判定）。