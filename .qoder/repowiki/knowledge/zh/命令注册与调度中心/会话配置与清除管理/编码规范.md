- 命令元数据与实现分离：每个命令目录下均包含 `index.ts` 用于声明静态属性，实际逻辑通过动态 `import()` 按需加载以优化启动性能。
- 统一的命令调用签名：本地 JSX 命令导出 `call` 函数接收 `onDone` 回调，普通本地命令接收 `(context, onDone)` 或类似上下文参数，保持接口一致性。
- 分层清除策略：将会话清除拆分为轻量级的 `clearSessionCaches`（仅清缓存）和重量级的 `clearConversation`（清状态、重置ID、执行钩子），供不同场景复用。