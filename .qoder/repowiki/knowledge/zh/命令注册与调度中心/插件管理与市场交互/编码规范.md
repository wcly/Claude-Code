- 采用受控状态机模式管理视图流转，通过 `viewState` 字段区分当前交互阶段（如 'plugin-list' 到 'plugin-details'）。
- 统一使用 `onComplete` 回调作为退出机制，将结果字符串或空值返回给父级命令处理器以结束会话。
- 在列表交互中广泛使用 `selectedIndex` 配合 `useKeybindings` 实现键盘导航，并通过 `figures` 库渲染选中态指针。
- 异步操作（如安装、刷新市场）后强制调用 `clearAllCaches()` 以确保后续读取获取最新数据。