- 所有步骤组件（Step Components）均采用纯函数式 React 组件，接收状态和回调作为 props，不包含内部复杂状态逻辑。
- 异步操作（如 API 调用、CLI 执行）统一在主控制器 `install-github-app.tsx` 中通过 `useCallback` 定义，并通过 props 传递给 UI 组件触发。
- 错误处理采用集中式状态更新模式，捕获异常后通过 `setState` 将 `step` 切换为 `'error'` 并展示 `ErrorStep`。