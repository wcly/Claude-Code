- 在 JSX 命令组件中使用 `useRef` 配合 `useEffect` 确保初始化逻辑（如状态切换）在组件生命周期内仅执行一次。
- CLI 子命令采用注册函数模式（如 `registerMcpAddCommand`），接收 `Command` 实例并链式配置选项与动作处理器。
- 在执行敏感配置写入（如 XAA setup）前，先进行全量参数校验，失败时直接退出以避免产生不一致的中间状态。