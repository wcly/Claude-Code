- 在传输层切换或重建时，必须捕获并传递 `lastSequenceNum` 以确保 SSE 流从断点续传，避免服务器重放全量历史消息。
- 所有出站消息在发送前均通过 `BoundedUUIDSet` 进行回声过滤，防止本地发出的消息在入站流中被重复处理。
- 使用 `FlushGate` 机制在初始历史消息刷写期间暂存实时消息，确保服务器端消息顺序的严格一致性。
- JWT 刷新采用 proactive 调度策略，在令牌过期前固定缓冲期（如 5 分钟）触发 `/bridge` 重新握手以更新 epoch。