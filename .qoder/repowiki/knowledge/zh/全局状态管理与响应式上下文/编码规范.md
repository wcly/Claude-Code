- 在 React 组件中消费全局状态时，必须使用带选择器（selector）的 `useAppState` Hook 以获取特定的状态切片，避免组件因无关状态变更而重新渲染。
- 状态更新逻辑倾向于使用函数式更新器（updater function）调用 `setState`，以确保基于最新状态进行不可变数据转换。
- 复杂的状态派生逻辑（如判断当前活跃的智能体）被提取到 `src/state/selectors.ts` 中的纯函数中，保持状态存储的简洁性与可测试性。