- **统一入口与动态路由**：以 `src/dev-entry.ts` 为启动枢纽，根据环境变量动态切换本地 REPL、云端 CCR 或直连模式，协调底层通信协议。
- **模块化能力注册**：通过 `commands/` 与 `tools/` 目录实现内置命令、插件技能及多智能体工具的懒加载与权限过滤，确保核心逻辑与扩展能力的解耦。
- **跨平台原生抽象**：利用 `shims/` 目录下的纯 TS 替代方案与 macOS 专属原生模块（`.node`），屏蔽 Yoga 布局、文件索引等底层差异，实现跨环境一致渲染。
- **响应式状态同步**：基于发布订阅模式的全局状态存储与 React Context，将查询执行、任务生命周期及 Buddy 伴侣状态实时同步至终端 UI 组件。