- **核心编排**：通过 `handleStopHooks` 协调查询结束时的生命周期事件，如记忆提取、任务分类及队友状态同步。
- **资源控制**：利用 `tokenBudget.ts` 实现基于令牌消耗和边际收益递减的自动停止决策机制。
- **依赖注入**：采用 `deps.ts` 定义 `QueryDeps` 接口，将模型调用、消息压缩等 I/O 操作抽象化以支持测试隔离。
- **配置快照**：通过 `config.ts` 在查询入口捕获不可变的会话 ID 和功能门控状态，确保执行上下文的一致性。