- 采用 Nominal Typing（名义类型）模式，通过字符串字面量交叉 `{ readonly __brand: '...' }` 防止不同业务 ID 之间的误用。
- 在 Schema 定义中普遍使用 `lazySchema` 包装器，以解决复杂对象图（如设置与插件之间）的循环引用问题。
- 常量文件保持零外部依赖（如 `src/constants/apiLimits.ts`），确保在构建早期或受限环境中可安全导入。
- 针对 discriminated union（判别联合）类型，提供配套的类型守卫函数（如 `isSyncHookJSONOutput`）以简化运行时的分支处理逻辑。