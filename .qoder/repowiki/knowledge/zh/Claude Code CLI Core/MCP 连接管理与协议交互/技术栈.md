- **协议 SDK**：`@modelcontextprotocol/sdk` (Client, SSE, StreamableHTTP, Stdio transports)。
- **认证标准**：OAuth 2.1, OIDC, RFC 8693 (Token Exchange), RFC 9728 (Protected Resource Metadata)。
- **运行时特性**：Bun-specific features (`bun:bundle`, `feature()` gates) 用于条件加载和性能优化。