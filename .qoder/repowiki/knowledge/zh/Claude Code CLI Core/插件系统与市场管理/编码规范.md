- 所有核心 Schema 定义均包裹在 `lazySchema` 中以解决循环依赖问题并确保按需初始化。
- 插件标识符统一采用 `plugin-name@marketplace-name` 格式，并通过 `PluginIdSchema` 进行正则约束。
- 文件系统操作优先使用带 `_DEPRECATED` 后缀的同步写入方法以确保原子性与刷新（flush），并在关键路径实现 V1 到 V2 格式的自动迁移逻辑。