- Use of `feature()` guards from `bun:bundle` to conditionally load heavy or internal-only modules at runtime.
- Implementation of `AnalyticsMetadata_I_VERIFIED_THIS_IS_NOT_CODE_OR_FILEPATHS` marker types to statically prevent accidental logging of sensitive user data.
- Adoption of `sideQuery` for all non-conversational API calls to ensure consistent OAuth fingerprinting and attribution headers.