- 在压缩请求中强制使用“仅文本”指令（NO_TOOLS_PREAMBLE），防止模型在单轮摘要任务中误调用工具导致失败。
- 采用 `<analysis>` 和 `<summary>` XML 标签结构引导模型输出，并在后续处理中剥离分析过程仅保留摘要内容。
- 压缩后通过 `postCompactCleanup` 统一重置微压缩状态、上下文折叠状态及各类缓存，确保新对话周期的环境纯净。
- 在处理部分压缩或消息切片时，通过 `adjustIndexToPreserveAPIInvariants` 向前回溯以确保 tool_use/tool_result 配对及思考块（thinking blocks）的完整性。