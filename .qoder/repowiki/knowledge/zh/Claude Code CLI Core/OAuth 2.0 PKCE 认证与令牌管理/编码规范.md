- 在 `client.ts` 和 `index.ts` 中广泛集成 `logEvent` 以追踪 OAuth 关键节点（如 token_exchange_success, auth_code_received）。
- 所有涉及敏感信息的 API 请求均在 `client.ts` 中显式设置 `timeout`（通常为 10s-15s）以防止无限挂起。
- 采用“配置优先”策略，在 `client.ts` 的 `populateOAuthAccountInfoIfNeeded` 中优先从环境变量或全局配置读取账户信息，减少不必要的网络请求。