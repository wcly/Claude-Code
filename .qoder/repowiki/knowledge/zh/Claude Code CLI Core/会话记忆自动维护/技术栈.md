通过 GrowthBook 动态特性门控与动态配置（`getFeatureValue_CACHED_MAY_BE_STALE` / `getDynamicConfig_CACHED_MAY_BE_STALE`）控制启用与阈值；使用 `lodash-es/memoize` 实现配置懒初始化；依赖 Anthropic API 的提示缓存机制，通过 `runForkedAgent` 保持 `CacheSafeParams` 一致以提升缓存命中。