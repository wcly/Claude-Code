- 每个提示对象采用统一结构：包含 id 标识符、异步 content 函数生成展示文本、cooldownSessions 定义冷却期、异步 isRelevant 函数判断当前上下文是否相关
- 提示内容函数接收 TipContext 参数以支持上下文相关的动态内容生成，如根据主题颜色格式化文本
- 相关性判断函数通过访问全局配置、环境变量、平台信息等来判断提示的适用场景，返回布尔值决定是否展示
- 冷却机制基于启动次数 (numStartups) 而非时间，通过比较当前启动次数与提示最后展示时的启动次数差值来判断是否超过冷却期