- 采用 AsyncGenerator 模式处理工具执行过程中的中间状态（如进度更新、钩子消息），实现流式响应与最终结果的统一交付。
- 实施防御性输入清洗，在权限校验前强制剥离模型可能注入的内部敏感字段（如 `_simulatedSedEdit`），确保执行安全。
- 建立细粒度的遥测埋点规范，对工具执行的各个阶段（权限等待、钩子耗时、实际执行）进行独立计时与 OTel 事件上报。
- 通过 `AbortController` 链实现复杂的取消逻辑，支持用户中断、兄弟工具错误级联取消以及流式回退时的资源清理。