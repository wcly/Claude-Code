- 对话框组件默认采用 'permission' 作为主题色，并通过 `color` 属性支持动态覆盖以适配不同业务场景。
- 底部操作指南区域默认渲染 Enter 确认与 Esc 取消提示，支持通过 `inputGuide` 属性传入自定义渲染函数以显示特定状态（如二次确认）。
- 权限类对话框采用三段式垂直布局：顶部为标题与副标题区，中部为可变内容区，底部保留边距，且默认隐藏左、右、下边框仅保留顶部圆角视觉。