- 技能定义采用 `SKILL.md` 文件配合 YAML frontmatter 的标准化格式，包含 name、description、type 等元数据。
- 记忆内容遵循四分类法（user, feedback, project, reference），并通过 `MEMORY.md` 作为索引文件指向具体的主题文件。
- 内置技能在首次调用时懒加载提取关联的参考文件到磁盘，并提供基础目录路径前缀以便模型检索。
- 路径解析逻辑包含严格的安全校验，防止路径遍历攻击，并对符号链接进行规范化处理以确保唯一性。