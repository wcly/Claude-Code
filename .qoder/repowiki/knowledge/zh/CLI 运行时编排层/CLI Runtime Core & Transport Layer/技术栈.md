- **CCR v2 Protocol**: Uses Server-Sent Events (SSE) for downstream event streaming and HTTP POST for upstream event batching, managed by `SSETransport` and `CCRClient`.
- **Batched Event Upload**: `SerialBatchEventUploader` provides serial, retry-aware, backpressure-controlled batch posting for high-volume event streams.