- Use `cliError` and `cliOk` from `exit.ts` for consistent process termination in subcommand handlers, ensuring centralized error logging and exit code management.
- Implement transport write operations with exponential backoff and jitter for retryable errors (429/5xx), while treating client errors (4xx except 429) as permanent failures.
- Utilize `logForDiagnosticsNoPII` for structured telemetry of transport lifecycle events (connect, reconnect, auth failures) to support observability without exposing sensitive data.