- 所有 UI 组件必须包裹在 <Box> 或 <Text> 等基础布局组件中，以触发 Yoga 布局计算，禁止直接在根节点渲染纯文本。
- 状态更新与渲染调度通过 throttled scheduleRender 进行防抖处理，确保在高频输入（如滚动、动画）下保持稳定的帧率（默认 ~60fps）。
- 使用 Context API (如 AppContext, StdinContext) 跨组件共享终端尺寸、输入流和退出逻辑，避免 Props 逐层透传。
- 自定义 Hook (如 useInput, useApp) 封装对底层 Ink 实例和事件发射器的访问，提供声明式的副作用管理。