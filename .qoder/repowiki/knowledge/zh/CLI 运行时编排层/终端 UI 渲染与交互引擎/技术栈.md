- **渲染后端**：使用 `react-reconciler` (Concurrent Mode) 将 React 组件树同步到自定义终端宿主环境。
- **布局引擎**：集成 `yoga-layout` (WebAssembly/JS binding) 实现 Flexbox 布局计算。
- **终端交互**：直接操作 `process.stdin/stdout`，支持 ANSI/VT100 控制序列、SGR 鼠标追踪和 Kitty 键盘协议。